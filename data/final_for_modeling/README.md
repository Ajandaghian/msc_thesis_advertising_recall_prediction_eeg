In this folder, you will find the final datasets that have been cleaned and preprocessed, ready for modeling. Each dataset is in `parquet` format and includes all necessary features for training machine learning models.

Here is the directory structure:
```
.
├── bin_femba_emb_df.parquet
├── bin_ts2vec_emb_df.parquet
├── data_columns.xlsx
├── key_status.xlsx
├── segment_df.parquet
├── segment_key_status.xlsx
├── test_ad_break_level_data.parquet
├── train_ad_break_level_data.parquet
└── tsegment_df.parquet
```